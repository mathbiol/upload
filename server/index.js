exports.handler = (event, context, callback) => {
    var fs=require('fs')
    // var path=require('path')
    // var aws=require('aws-sdk')
    // var conn=JSON.parse(fs.readFileSync('connection.json','utf8'))
    // TODO implement
    callback(null,'upload att '+Date());
};